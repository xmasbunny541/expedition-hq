# Errors

Store repeated or high-signal failure patterns and their fixes.

Do not log every transient command failure. Log errors when a future agent would benefit from knowing the exact cause, evidence, and recovery path.

