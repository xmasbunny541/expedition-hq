# First Codex Runbook

1. Read `AGENTS.md`.
2. Run `python scripts/validate_seed.py`.
3. Implement the backend seed loader.
4. Implement `GET /agents`, `GET /expeditions`, `GET /events`, `GET /milestones`, and `GET /incidents`.
5. Implement `POST /events`.
6. Render seed data in the Vite app.
7. Add a bootstrap field report.
8. Keep first version read-only.
